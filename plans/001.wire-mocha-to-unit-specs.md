# 001 — Wire real mocha to the hand-rolled fees spec

## Build order

1. Add devDependencies: `mocha`, `@types/mocha`, `ts-node` (none were
   present).
2. Convert `fees.spec.ts` in place: redefine the file's `expect` helper
   to call `it(label, () => {...})`, wrap the body in one top-level
   `describe(...)`, leave every individual assertion call site
   untouched — safe here since the whole file is synchronous.
3. Add an npm `"test"` script: mocha over `src/**/*.spec.ts` via
   `ts-node/register`, `TS_NODE_TRANSPILE_ONLY=1` (matches
   `exchange-connector-sh`'s script in this same sweep).
4. Add `.github/workflows/test.yml`: PR + push to `main`, Node 22,
   `npm ci`, `npm test`.
5. Verify `npm test` green: 10/10, matching the baseline (§4).

## Blast radius

1. Other call sites: `paperOrderFee` itself is untouched — only the
   test file, devDependencies, and CI file changed.
2. `core/` submodule: this repo has no `.gitmodules` — it *is* what
   `paper-trading` submodules as `core/`. That repo's own CI is a
   separate, later change.
3. CLAUDE.md contracts: no application code touched.
4. Other tests: none exist elsewhere.

## Traceability

| Spec section | Test / file |
|---|---|
| §2.1/§2.3 | `src/order/fees.spec.ts`, `package.json` `"test"` |
| §2.2 | `.github/workflows/test.yml` |
